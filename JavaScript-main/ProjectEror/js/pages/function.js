/*function chek(){
    if(!event.target.classList.contains('but')) return;
    if(event.target.classList.contains('CA')) return;

    conc.textContent = '';

    const Number = event.terget.textContent;

    if(Num.includes(Number)){
        a += Number;
        console.log(a,b,sign);
    }
}
*/
function DivineLight(){
    a = '';
    b = '';
    sign = '';
}

